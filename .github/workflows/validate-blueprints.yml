name: Validate blueprints

on:
  push:
    paths:
      - "blueprints/**/*.yaml"
      - "blueprints/**/*.yml"
  pull_request:
    paths:
      - "blueprints/**/*.yaml"
      - "blueprints/**/*.yml"

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Install PyYAML
        run: pip install pyyaml

      - name: Validate blueprint YAML syntax
        run: python3 scripts/validate_blueprints.py
